import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'ChatAI Pro — AI Chatbot Agency Dashboard',
  description: 'Monitor your AI chatbot performance, client analytics, ROI metrics, and engagement in real time.',
  keywords: ['AI chatbot', 'agency dashboard', 'client management', 'analytics'],
  authors: [{ name: 'ChatAI Pro' }],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased bg-background text-foreground`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
